CREATE TABLE location (user TEXT NOT NULL, timestamp TEXT NOT NULL, longitude REAL NOT NULL, latitude REAL NOT NULL);
.tables (lists all tables)
 INSERT INTO location VALUES ("sri", "2022-09-13T18:20:55.275Z", -27.47758514247441, 153.02939690287167);
SELECT * FROM location;
.quit
.quit
.table
CREATE TABLE location_updates (timestamp TEXT NOT NULL, x-coord REAL NOT NULL, y-coord REAL
NOT NULL);
CREATE TABLE location_updates (timestamp TEXT NOT NULL, x_coord REAL NOT NULL, y_coord REAL
NOT NULL);
.table
.quit
.table
CREATE TABLE TEST_location (timestamp TEXT NOT NULL, x_coord REAL NOT NULL, y_coord  REAL NOT NULL);
SELECT * FROM TEST_location;
CREATE TABLE TEST_door(doctor TEXT NOT NULL, room, REAL NOT NULL, timestamp TEXT NOT NULL);
.tables
CREATE TABLE TEST_door(doctor TEXT NOT NULL, room TEXT NOT NULL);
CREATE TABLE TEST_door2(doctor TEXT NOT NULL, room TEXT NOT NULL);
SELECT * FROM TEST_door2
;
.quit
.tables
sqlite> CREATE TABLE room_movement (timestamp TEXT NOT NULL, x_coord REAL NOT NULL, y_coord REAL NOT NULL);

sqlite> CREATE TABLE room_movement(timestamp TEXT NOT NULL, x_coord REAL NOT NULL, y_coord REAL NOT NULL);

sqlite> CREATE TABLE location (user TEXT NOT NULL, timestamp TEXT NOT NULL, longitude REAL NOT NULL, latitude REAL
NOT NULL);

.tables
sqlite> CREATE TABLE location (user TEXT NOT NULL, timestamp TEXT NOT NULL, longitude REAL NOT NULL, latitude REAL
NOT NULL);

sqlite> CREATE TABLE room_movement(timestamp TEXT NOT NULL, x_coord REAL NOT NULL, y_coord REAL NOT NULL);

CREATE TABLE room_movement (timestamp TEXT NOT NULL, xcoord REAL NOT NULL, ycoord REAL NOT NULL);
CREATE TABLE doctor_movement (Doctor TEXT NOT NULL, RoomNumber REAL NOT NULL);
.tables
SELECT * FROM doctor_movment
;
SELECT * FROM doctor_movement;
SELECT * FROM room_movement;
